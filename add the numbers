#include <stdio.h>
int main ()
{
int v1, v2,sum;
printf("enter a  first number ");
scanf("%d",&v1);
printf("enter a second number");
scanf("%d",&v2);
sum = v1 + v2;
printf ("The sum of %i and %i is= %i\n", v1, v2, sum);
return 0;
}
